{
  "name": "textexpanderexporter",
  "version": "1.0.0",
  "description": "Exports TextExpander snippets to other formats.",
  "main": "bin/textexporter",
  "scripts": {
    "clean": "cat .cleanrc | sed -E '/^#.*$/ d' | sed '/^\\s*$/ d' | xargs rm -rf",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nick Williams",
  "license": "BSD-2-Clause",
  "dependencies": {
    "lodash": "^4.17.10",
    "simple-plist": "^0.3.0"
  }
}
